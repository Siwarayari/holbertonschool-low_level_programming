#include "main.h"
 /**
 *_memcpy - copies memory area.
 *@dest: destination.
 *@src: memory area.
 *@n: bytes from memory area.
 *Return: dest.
 */
char *_memcpy(char *dest, char *src, unsigned int n);
{
	unsigned int i;

	for (i = 0; i < n; i++)

	return (dest);
}
